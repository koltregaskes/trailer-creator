# Setup

1. Open `index.html` in a modern browser.
2. If the browser can read the adjacent `.txt` files, the page will load them directly.
3. If local file access blocks those reads, the page falls back to bundled content so it still works offline.
